---
import '../styles/global.css';

interface Props {
  title: string;
  description?: string;
}

const { title, description = 'Torneo FIFA Xtreme 2025' } = Astro.props;
---

<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title} | Xtreme 2025</title>
    <meta name="description" content={description} />
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
  </head>
  <body>
    <!-- Header mejorado -->
    <header class="bg-gradient-to-r from-gray-900/95 to-gray-800/95 backdrop-blur-sm border-b border-gray-700 sticky top-0 z-50">
      <div class="container py-4">
        <div class="flex items-center justify-between">
          <div class="flex items-center space-x-4">
            <div class="w-12 h-12 bg-gradient-to-r from-blue-500 to-purple-600 rounded-xl flex items-center justify-center">
              <span class="text-white font-bold text-xl">⚽</span>
            </div>
            <div>
              <h1 class="text-2xl font-bold text-white m-0">FIFA Xtreme 2025</h1>
              <p class="text-sm text-gray-400">Torneo Profesional</p>
            </div>
          </div>
          <nav>
            <ul class="flex items-center space-x-1">
              <li><a href="/" class="nav-link">🏠 Inicio</a></li>
              <li><a href="/jugadores" class="nav-link">👥 Jugadores</a></li>
              <li><a href="/sorteo" class="nav-link">� Sorteo</a></li>
              <li><a href="/grupos" class="nav-link">⚽ Grupos</a></li>
              <li><a href="/torneo" class="nav-link">🏆 Torneo</a></li>
              <li><a href="/estadisticas-avanzadas" class="nav-link">📊 Stats</a></li>
            </ul>
          </nav>
        </div>
      </div>
    </header>
    
    <!-- Main content con padding mejorado -->
    <main class="container py-8 px-4 sm:px-6 lg:px-8">
      <slot />
    </main>
    
    <!-- Footer mejorado -->
    <footer class="bg-gradient-to-r from-gray-900 to-black border-t border-gray-700 py-8 mt-16">
      <div class="container">
        <div class="flex flex-col md:flex-row justify-between items-center space-y-4 md:space-y-0">
          <div class="flex items-center space-x-4">
            <div class="w-10 h-10 bg-gradient-to-r from-blue-500 to-purple-600 rounded-lg flex items-center justify-center">
              <span class="text-white font-bold">⚽</span>
            </div>
            <div>
              <p class="text-white font-semibold">&copy; 2025 FIFA Xtreame Tournament</p>
              <p class="text-gray-400 text-sm">Sistema de torneos profesional</p>
            </div>
          </div>
          <div class="flex items-center space-x-6">
            <span class="text-gray-400 text-sm">Desarrollado con</span>
            <div class="flex items-center space-x-2">
              <span class="text-red-400">❤️</span>
              <span class="text-gray-300">usando</span>
              <span class="text-blue-400 font-semibold">Astro</span>
              <span class="text-gray-300">+</span>
              <span class="text-purple-400 font-semibold">Prisma</span>
            </div>
          </div>
        </div>
      </div>
    </footer>

    <style>
      .nav-link {
        @apply px-4 py-2 rounded-lg text-gray-300 hover:text-white;
        @apply hover:bg-white/10 transition-all duration-200;
        @apply border border-transparent hover:border-gray-600;
        @apply backdrop-blur-sm;
      }

      .nav-link.active {
        @apply bg-blue-600/20 text-blue-400 border-blue-500/30;
      }

      .container {
        @apply max-w-7xl mx-auto px-4;
      }
    </style>
  </body>
</html>
